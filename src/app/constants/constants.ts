// const serverPrefix = `https://visual-search-research-797f1f0ba0ac.herokuapp.com`;
const serverPrefix = '';

export const Constants = {
  apiPaths: {
    get_form_data: `${serverPrefix}/get-info-form`,
    save_form_data: `${serverPrefix}/save-form-data`,
    game: `${serverPrefix}/game`
  }
}
